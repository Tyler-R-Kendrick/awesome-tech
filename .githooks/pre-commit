#!/usr/bin/env sh
set -eu

# Fast, local pre-commit gate (see DX.md / the Monorepo DX Playbook).
# Whitespace and merge-conflict-marker hygiene on staged content.
git diff --cached --check

# Structural conventions for the docs knowledge tree (runs in milliseconds).
bun run konsistent
